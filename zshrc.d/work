DEV="/home/tom/dev"
STYLEME="${DEV}/styleme"

alias dev="${DEV}"

alias sm="${STYLEME}/styleme"
alias smt="${STYLEME}/templates"
alias smm="${STYLEME}/media"
alias smc="${STYLEME}/contrib"

alias m="${STYLEME}/styleme/manage.py"

alias s="m shell"
alias dbs="m dbshell"


for MACHINE in burberry ford galliano hackett mcqueen conran westwood sander
do
    alias ${MACHINE}="ssh tom@${MACHINE}.adm.thread.com"
done
